# Classroom v8.2 — Today Schedule Fix

This package repairs the Today Schedule panel while preserving existing local data.

Upload every file and the `assets` folder to the GitHub repository root. Do not clear Chrome site data.

Open after deployment:

`https://qwl-cloud.github.io/Classroom/?v=8.2-today-fix`
